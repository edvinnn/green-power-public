/*
    Handles all the updating of the components on the dashboard when a user is signed in. 
    Hence this script should only be called from where a user can be signed in.
*/
(function($) {
    "use strict";

    // Handle all dashboard related updating here

})(jQuery);
